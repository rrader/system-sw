sudo umount -f /mnt/; make && sudo make reload && sudo mount -o loop -t ffs mkfs.ffs/img /mnt

Works:

  - write/append file
  - read file
  - list dir
  - remove file
  - mkdir
  - rmdir
  - hard links
  - symbolic links

TODO:

  - rename