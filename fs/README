sudo umount -f /mnt/; make && sudo make reload && sudo mount -o loop -t ffs mkfs.ffs/img /mnt

to write
mark_buffer_dirty(bh);
sync_dirty_buffer(bh);

TODO:

  - write to file (append)
  - add file to directory